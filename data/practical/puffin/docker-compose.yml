
services:
  game-server:
    image: server/2023q-pwn-puffin:latest
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    networks:
      attacknet:
        aliases:
          - target.server.io
    ports:
      - 31140:31140

networks:
  attacknet:
    external: true